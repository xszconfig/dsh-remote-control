# DSH 远程连接排障记录 + Remote Control App 经验库

> 事件日期：2026-08-25（现象）~ 2026-08-26（修复完成）
> 场景：Daniel 人在惠州，Mac 在深圳家中，用手机 dsh Remote App 远程控制 Mac 上的 DeepSeek Harness（dsh）开发 APP
> 涉及项目：`~/code/dsh-remote-control`（Android 客户端）、`~/code/dsh-remote-control-bridge`（dsh 插件）
> 定位：本文档 = 排障复盘 + 通用经验库，未来做任何 Remote Control APP 时先读此文档

---

## 一、问题现象

- 手机端（dsh Remote / Devin Control）与桌面端（dsh web）**两边都跑起来了**，但手机扫码连接不上
- dsh web 页面显示「可连接移动端」并生成配对二维码，**扫了之后没连上**，App 端提示「所有候选地址均连接失败」
- 前一天（8/23）同一套 App 在同一台 Mac 上是**能连上的**（USB + 反代），第二天换到户外环境就断了

## 二、根因分析（结论先行：不是服务挂了，是网络路径设计使然）

### 2.1 服务端本身完全健康（排查第一步排除）
```
lsof -iTCP:3080 -sTCP:LISTEN  →  node dsh web 在听
curl 127.0.0.1:3080/remote/health → {"ok":true,"version":"0.3.0","sessions":2}
node WS 连 ws://127.0.0.1:3080/remote/ws?pair=<token> → OPEN ✓（服务端配对握手正常）
```

### 2.2 真正的根因：dsh web 只监听 127.0.0.1（loopback）
- dsh web 的 Web 服务**只绑定本机回环地址**，`--host 0.0.0.0` 被安全策略**硬拒**：
  ```
  error: --host 0.0.0.0 is intentionally not supported yet for safety:
  it would expose remote code execution to the network; use 127.0.0.1 instead
  ```
- bridge 生成的配对二维码里有两个候选地址（`bindHost=127.0.0.1` 时）：
  - `ws://127.0.0.1:3080/remote/ws?pair=xxx` → 手机扫到 127.0.0.1 = **手机自己**，必然连不上
  - `ws://192.168.1.110:3080/remote/ws?pair=xxx` → 局域网 IP，但**服务没监听局域网**，必然连不上
- 两个候选全部失败 → App 报「所有候选地址均连接失败」
- 8/23 能连上的原因：当时手机 **USB 插着 Mac + `adb reverse tcp:3080 tcp:3080`**，127.0.0.1 经 USB 反代指向 Mac → 通

### 2.3 教训
- **「两边都跑起来」≠「能连上」**：服务监听边界、网络拓扑（NAT/公网）、协议（ws vs wss）都会让两端都活着的连接失败
- loopback-only 架构下，**局域网直连和公网直连都是设计上不可能的**，必须走反代/隧道/中继
- 排查顺序：先验证服务健康（health + 握手），再分析网络路径（监听地址 / 二维码内容 / 手机可达性），最后才怀疑服务端

## 三、解决方案（已实施并验证）

### 3.1 总体思路
```
手机(惠州, 移动网络)
   ↓ wss:// 公网隧道（加密）
Cloudflare edge（trycloudflare.com）
   ↓ cloudflared 本地进程
Mac 127.0.0.1:3080（dsh web + bridge）
```

### 3.2 步骤
1. **开公网隧道**（把 loopback 服务暴露成公网地址）：
   ```bash
   nohup cloudflared tunnel --url http://127.0.0.1:3080 > /tmp/dsh-tunnel.log 2>&1 &
   grep -oE "https://[a-z0-9-]+\.trycloudflare\.com" /tmp/dsh-tunnel.log | head -1
   # → https://thumbs-output-accommodate-copies.trycloudflare.com
   ```
2. **配环境 token 鉴权**（防公网裸奔）：
   ```bash
   TOKEN=$(openssl rand -hex 24)
   echo "$TOKEN" > ~/.dsh/dsh-remote-token.txt && chmod 600 ~/.dsh/dsh-remote-token.txt
   DSH_REMOTE_TOKEN="$TOKEN" nohup dsh web > /tmp/dsh-web.log 2>&1 &
   ```
   - 不配 token 时 bridge 的鉴权是 `open`（无凭证直接放行），依赖 loopback 绑定兜底 → 公网下等于裸奔
   - 配了 token 后：无凭证 → 401；`?token=` / `Authorization: Bearer` 带对 token → 放行
3. **补 bridge 漏洞**（堵「公网免 token 铸造配对 token」）：
   - 原实现：`/remote/pair-info|pair|devices|connected` 只查 `isLoopback(req)`（socket 地址）
   - 漏洞：cloudflared 本地转发使 socket 地址=127.0.0.1 → 公网任意人可无限铸造 pair token
   - 补丁（`src/index.ts`）：新增 `isLoopbackHostHeader` + `allowLocalOrEnvToken(req, res, envToken)`
     - 真·本机放行：socket loopback **且** Host 头也是 loopback（127.0.0.1/localhost/::1）
     - 非本机：必须带 `Authorization: Bearer <envToken>`
   - 重建：`pnpm exec tsc -p tsconfig.json`；**插件目录与源码是硬链接**（同 inode），重建即自动生效
4. **生成配对二维码**（含 wss 地址 + token）：
   ```
   wss://thumbs-output-accommodate-copies.trycloudflare.com/remote/ws?token=<ENV_TOKEN>
   ```
   - App 扫码流程支持：JSON payload（`{v:1,t:'dsh-remote',urls:[...]}`）或裸 `ws://`/`wss://` URL
5. **手动连接参数**（App「手动连接」只有 Host/Port/Token 三个字段，协议**写死 ws://**）：
   - Host：隧道域名；**Port：80**（ws:// 走 80；填 3080 连不上）；Token：env token
   - ⚠️ 手动=ws:// 明文，token 会在公网传输中可见 → 户外优先扫码（wss 加密）

### 3.3 验证清单（全部实测通过）
| 检查项 | 预期 | 实测 |
|---|---|---|
| 本机 health | ok | ✓ |
| 本机 pair-info（真本机 Host） | 200 | ✓ |
| 本机 pair-info（伪造隧道 Host） | 403 | ✓ |
| 本机 ws 无 token | 401 | ✓ |
| 隧道 pair-info 无 token | 403 | ✓ |
| 隧道 pair-info + Bearer | 200 | ✓ |
| 隧道 wss + ?token= | WS OPEN | ✓ |
| 手动 ws://:80 + token | WS OPEN | ✓ |

### 3.4 当前运行状态（2026-08-26）
- dsh web：PID 90844（带 token 版），127.0.0.1:3080，健康，sessions 在磁盘
- cloudflared：PID 90115，隧道 URL 见上
- token：`~/.dsh/dsh-remote-token.txt`（48 hex）；二维码：`~/.openclaw/workspace/tmp/dsh-remote-qr.png`
- **隧道 URL 是临时的**（cloudflared 重启/掉线即变）→ URL 变了只需重新生成二维码，token 不变

## 四、后续优化措施

### 4.1 稳定性（最重要）
- ⏳ **Tailscale（首选长期方案）**：Mac + 手机入同一 tailnet，dsh web 仍绑 127.0.0.1，用一个中继（socat/node）监听 Tailscale IP → 转发 3080；**私网加密、IP 稳定、无公网暴露**，彻底解决「URL 临时」问题
- ⏳ launchd 常驻：dsh web + cloudflared 开机自启、崩溃自拉起（当前 nohup 在 Mac 重启后失效）
- ⏳ 健康巡检：定时 `curl 127.0.0.1:3080/remote/health` + 隧道可达性，异常时通知

### 4.2 App 端（dsh Remote）
- ⏳ 手动连接支持 **wss://**（当前 `buildUrl` 写死 `ws://`，手动模式无法加密）
- ⏳ 手动连接支持**粘贴完整 URL**（含 pair/token 参数），减少参数拆填
- ⏳ **断线自动重连**（指数退避），弱网（户外移动网络）体验关键
- ⏳ 连接页展示「当前尝试的地址 + 失败原因」，别只报「所有候选地址均连接失败」
- ⏳ token 支持**轮换**（服务端换 token 后旧设备失效，App 提示重新配对）

### 4.3 安全
- ⏳ token 定期轮换（当前为固定 env token）
- ⏳ 公网隧道 URL 保密（勿外传；已加 token + Host 校验双保险）
- ⏳ 长期用 Tailscale 替代公网隧道，消灭公网暴露面

### 4.4 运维速查
- 隧道 URL 变了 → 重新执行 3.2 第 1 步取新 URL → 重新生成二维码（token 不变）
- dsh web 挂了 → `DSH_REMOTE_TOKEN=$(cat ~/.dsh/dsh-remote-token.txt) nohup dsh web &`
- 杀 dsh web 必须用**精确 PID**（`kill <pid>`），不要 `pkill -f "dsh web"`（会误杀主进程——8/26 已踩坑）

## 五、Remote Control App 通用经验库（跨项目可复用）

### 5.1 架构决策
- **控制面与数据面分离**：手机是控制面，代码/密钥/工作区永驻桌面端；桌面端收敛为 bridge 插件，不碰内核
- **服务端只监听 loopback + 隧道/反代出口**：安全（防远程代码执行）与可达性解耦
- 协议选型：WSS（加密）优先；客户端若只支持 ws://，就只能在可信网络用

### 5.2 网络拓扑排查清单（连不上时按序检查）
1. 服务端进程/端口活着？（`lsof -iTCP:PORT -sTCP:LISTEN`）
2. 服务端健康接口？（health/ping）
3. 服务监听的是 127.0.0.1 还是 0.0.0.0？（决定局域网/公网可达性）
4. 客户端拿到的地址列表，逐个分析可达性：
   - 127.0.0.1 → 客户端自己（除非有 adb reverse / 端口转发）
   - 局域网 IP → 需服务监听局域网 + 同网段
   - 公网域名 → 需隧道/端口映射
5. 协议：ws:// 明文 vs wss:// 加密，端口（80 vs 443 vs 自定义）
6. 鉴权：无 token 是否被拒？token 是否在 URL 里明文传输？

### 5.3 安全默认值
- 任何「loopback 信任」假设在**反向隧道**场景下都失效（socket 地址是本机，来源是公网）→ 必须叠加 Host 头校验或 token 鉴权
- 配对 token 必须短 TTL + 随机；设备 token 持久化但可吊销
- 暴露 agent/执行环境 = 远程代码执行入口，优先走私网（Tailscale），公网隧道是最后手段

### 5.4 配对/UX 设计
- 二维码配对（payload 含 serverId + 候选 urls + 过期时间）体验最好：零输入、自动多地址回退
- 设备指纹记忆：连过的设备进列表，服务在线即可一键重连（已实现于 `DeviceStore`）
- 候选地址按「最优先→最次」排序（127.0.0.1 → Host 头 → 局域网 IP），客户端逐个尝试

### 5.5 测试清单（发布前必过）
- [ ] 本机回环连接（localhost + 正确 token）
- [ ] USB + adb reverse 场景（127.0.0.1 可达性）
- [ ] 局域网 Wi-Fi 场景（若服务支持 0.0.0.0）
- [ ] 公网隧道场景（wss + token）
- [ ] 无 token / 错 token → 拒绝
- [ ] 断网/杀进程后重连行为
- [ ] token 轮换后旧连接失效

---
*文档由 Leo 整理（2026-08-26）。后续任何 remote control 项目的排障结论请追加到第五节，保持经验库自增长。*
