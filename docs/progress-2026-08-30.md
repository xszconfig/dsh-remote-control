# 进展记录 2026-08-30（全天）

> 覆盖 dsh-remote-control（Android 客户端）+ dsh-remote-control-bridge（服务端插件）两个仓库。
> bridge 版本 0.10.3 → **0.11.9**；冒烟回归 48 → **76 断言（76/76）**。
> 主题：从消息底座走向 IDE 级能力——LSP 代码智能、Goal/任务列表、受控调试、服务端权威计时。
> （08-31 00:02 的迭代总结文档提交并入本日；选型论证另见 `debug-protocol-cdp-vs-dap.md`。）

## 提交时间线（版本里程碑）

| 时间 | 版本 | 里程碑 |
|---|---|---|
| 00:17 | 0.10.3 | inbox/spliced 滞后一拍修复（queueMicrotask 延迟广播） |
| 00:59 | 0.10.5 | 历史分页按投影行翻页 + 工具描述优先 description |
| 01:32 | 0.10.7 | 思考流式 think_delta（250ms→100ms 节流） |
| 02:36 | 0.10.9 | Edit/Write 工具调用下发 DiffCallView.diffs |
| 08:20 | — | README 对齐 0.10.9 |
| 12:43 | 0.11.0 | LSP 代码智能首版（Agent 编辑文件 → 诊断广播手机） |
| 13:29 | 0.11.1 | 重启自动续跑 v1（work.json + 自报版本） |
| 14:31 | 0.11.4 | LSP 真机诊断打通（publishDiagnostics 能力 + tsserver.js 定位） |
| 16:09 | 0.11.5 | 会话隔离（Deep Diving/诊断按 sessionId，openSession 全量重置） |
| 16:41 | 0.11.6 | Goal 面板（goal/change 投影广播 + 订阅携带） |
| 17:27 | 0.11.7 | Node Inspector 调试后端 + Kotlin 通道 |
| 18:53 | — | 官方 JetBrains kotlin-lsp 接入（0 诊断四层根因） |
| 19:48 | 0.11.8 | 服务端计时/todos/lsp_query 工具/排队快照持久化 |
| 20:56 | — | Kotlin 对齐 oh-my-pi 初始化时序 + 软链路径根因（六层全修） |
| 21:34 | — | goal/todos 软读改 ctx.get（cordis inject 陷阱修复） |
| 22:16 | — | 自动续跑根治版（持续重试+指纹幂等+会话归属） |
| 22:28 | 0.11.9 | Agent 调试工具 debug_start/debug_command |
| 22:40 | — | 协作铁律写入 AGENTS.md（两仓库） |
| 22:54 | — | 调试注册表残留修复（"已有调试进程在运行"卡死） |
| 23:07~23:42 | — | Deep Diving 对齐 DSH Web（turn_status 全轮次 + 15s 时钟 + turnSince） |

## 上半场（00:00–13:00）：消息底座打磨

### 🐛 修复的 Bug（7 个）
1. **排队消息同步慢/状态错位**：inbox 落库先于投影更新，观察者读到变更前队列 → `queueMicrotask` 延迟广播
2. **历史分页卡死**：109 万事件大会话按原始窗口翻页整页投影为空 → 按投影行翻页
3. **最新消息"丢失"**：逆序扫描忘 reverse → 消息顺序颠倒
4. **翻页丢行**：`distinctBy{seq}` 把同 seq 的 think/正文去重掉一行 → 改 `seq+type`
5. **发送后键盘闪**：先 clearFocus 再收键盘
6. **重启流程翻车**：pgrep 匹配不到/launchd 抢端口 → 简化为 dshweb-restart 同款模式
7. **工具描述显示错误**：Bash 展示 Command 而非 Description → 优先 description

### ✨ 新增功能
思考流式一行刷新、长按复制、Deep Diving 品牌蓝指示条（初版）、工具结果折叠、Markdown 渲染（mikepenz 0.28.0）、Code Diff（红删绿增）、输入框蓝边框、自动收键盘、找回 17 条丢失排队消息、dsh-restart skill。

## 下半场（13:00–24:00）：IDE 级能力

### LSP 代码智能（6 语言）
- TS/JS（tsserver）、Python（pyright）、Rust（rust-analyzer）、C/C++（clangd）、**Kotlin（官方 JetBrains kotlin-lsp，IntelliJ 内核）**
- Kotlin 0 诊断**六层根因**：pull 诊断模式（只推空 publishDiagnostics）→ 主动 textDocument/diagnostic；KotlinProblemHighlightFilter 需 content root → 向上找 gradle/pom 项目根；`initialized` 误带 id → 改 notify；didChange version Date.now() 溢出 int → 递增小整数；路径未 realpath（/tmp→/private/tmp 软链）→ 统一 realpath + docPaths 映射；ready-for-test 后强制重同步 + 长窗口拉取
- 对齐 oh-my-pi 初始化时序：全量 CLIENT_CAPABILITIES + workspaceFolders + didChangeConfiguration
- **lsp_query Agent 工具**：diagnostics/hover/definition/references（真实 server 四动作验证）

### Debug 受控调试（CDP / Node Inspector）
- `node --inspect-brk=0` → Debugger/Runtime 域驱动断点/暂停/调用栈/变量/单步/跳出/输出
- 踩坑：-brk 需 runIfWaitingForDebugger 释放；入口暂停单次自动恢复（双 resume 撞错）；断点 URL realpath；暂停帧 url 经 scriptId 查 scriptParsed 表；console 双通道去重；"Waiting for the debugger" 主动断开；**注册表残留修复**
- 控制面三层：REST（Agent 启动）+ WS debug_command（手机按钮）+ debug_start/debug_command 工具（0.11.9）
- 手机调试面板：开发状态行聚合徽标 → 抽屉（诊断/调试 Tab：调用栈切换、变量树惰性展开、继续/单步/跳出/停止、输出区）
- 选型：CDP-only（论证见 `debug-protocol-cdp-vs-dap.md`；DAP seam 预留）

### Goal / 任务列表
- Goal：ctx.goals.get + goal 投影；goal/change → goal_update 广播
- 任务列表：todos 投影（todo/write 全量快照）；todo/write → todos_update；**turn/start 广播空清单**（按轮次生命周期，不残留旧任务）
- **cordis inject 陷阱修复**：属性直读抛 "cannot get property without inject" → ctx.get 软读（此前真机 Goal/todos 全废）
- 手机 UI：Goal 折叠单行可展开；任务列表条（Deep Diving 下、Goal 上，≤3 条滑动、状态图标）

### Deep Diving（对齐 DSH Web 语义）
- 读 dsh-client-ui-conversation 源码对齐：锚点 = 当前 OPEN 轮次起点；标签整个轮次显示；**≥15s 才显示时钟**
- 服务端权威：turn_status(open/closed) + 每秒 tick（turnStarts 驱动）；订阅携带 turnSince（中途切入立即显示）
- 客户端删除本地计时与轮次边界推断；格式：<60s N秒 / ≥60s X分Y秒 / ≥60min X小时Y分

### 排队 / 自动续跑
- 排队面板 ≤3 条滑动；**跨重启快照恢复**（work.json queues + id/文本去重 + 幂等清理）
- 自动续跑根治版：每 20s 持续重试 + 指纹幂等 + agent/status 触发 + sessionId 归属（修复"重启后恢复不稳定"）
- 遗留（08-31 待做）：唤醒速度优化（目标挂载后 ≤3s）

### UI 细节
- 发送按钮 → Agent 运行中变红色终止按钮（interrupt）；顶栏去掉「断开」、标题加宽

## 🏗 架构与协作
- 单向数据流强化：计时/队列/任务列表全部以服务端投影为唯一事实源
- 协作铁律写入两仓库 AGENTS.md：重大架构/路线/选型决策必须先问用户；只本地 commit；重启先确认等 8 条
- 调试协议选型文档 `debug-protocol-cdp-vs-dap.md`

## 📊 质量数据（48h 编译统计）
| 项目 | 编译次数 | 错误次数 | 错误率 | 最高频错误 |
|---|---|---|---|---|
| App (Kotlin) | 89 | 26 | 29.2% | Unresolved reference（8 次） |
| Bridge (TS) | 85 | 13 | 15.3% | TS2322/2345/2554 类型族（7 次） |
错误主因："新功能扩展后消费面没同步改全"（缺分支/import/类型对齐），绝大多数一次修复通过。

## ✅ 验收状态
- 服务端自测全过：冒烟 76/76；debug 真机同路径 8/8；lsp_query 四动作；Goal 订阅链路；自动续跑指纹证据
- 真机交互验收：排队 5 条（插队/删除/3 条滑动）、任务列表 5 条（图标/滚动/清空）
- ⏳ 待用户视觉验收：Deep Diving 标签全轮次 + 15s 时钟 + 任务列表轮次清空
- ⏳ 待做（08-31）：自动续跑速度优化（≤3s 唤醒）
- 推送状态：按用户指示已推送一批（bridge b13f142 / app a4e1202）；其后 6 个本地 commit 待指示
