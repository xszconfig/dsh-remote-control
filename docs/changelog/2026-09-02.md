# 更新日志 2026-09-02

## ✨ 新功能
- **feat(app)**：子代理列表副标题元信息 + 上限 10 条滚动——`SessionSummary` 增 `lastMessageAt/runDurationMs/totalTokens`（可空，旧 bridge 兼容）；新增 `MetaFormat.kt` 纯函数（时长/token/元信息段格式化，「·」分隔、缺省段跳过）；下拉副标题 = 状态标志 + 元信息（`AnnotatedString` 保留状态色、可换行多段）；列表最多同屏 10 条（`LazyColumn` + `heightIn` 上限），超出上下滚动；`MetaFormatTest` 6 例（11feae1）
- **feat(bridge)**：子代理列表元信息投影——`SessionSummary` 增量加最后消息时间/运行时长/token：`lastMessageAt` = 末事件时间（live）/`max(createdAt,lastPromptAt)`（冷），`runDurationMs` = `sessionStats`(llmMs+toolMs)，`totalTokens` = `tokenUsage` 四桶之和；三者跨重启持久化，旧部署/headless 缺省时客户端不渲染对应段；smoke 加兼容断言，BRIDGE_VERSION 维持 0.13.0（1ad349d）
- **feat(app)**：消息列表「回到底部」悬浮按钮——最新消息不可见时显示（reverseLayout 下 liveThink 占 index 0 时最新在 index 1），点击瞬间 `scrollToItem(0)` + 置回 followBottom 持续贴底；48dp 圆钮与发送按钮一致、半透明底、Canvas 自绘下箭头、淡入淡出；位于消息列表右下角（Deep Diving 上方右对齐）；可见性判据抽纯函数，`JumpToBottomTest` 8 例（4184b59）

## 🐛 修复
- **fix(bridge)**：排队消息防抖窗口竞态重复注入——`restoreQueueIfLost` 与 2000ms 快照防抖竞态：消息消费后 2s 窗口内磁盘快照仍含旧值，恢复巡检（resumeTick/agent:status running）把「已消费」误判为「重启丢失」→ `followup` 重注入；改为对比前先 `flushQueueSnapshot` 挂起写入让磁盘追上真实队列；`queueSnapTimers` 改存 `{timer,items}`；smoke 加防抖窗口回归（cc35dc8）

## 🎯 重要决策与规则
- 两仓库 AGENTS.md 新增「装机后自动执行真机验收工作流」铁律（app 铁律 11 / bridge 铁律 12）：装机 → 启动冒烟 → 真机自测 → 发现问题自动修复 → 复验 → 表格交付人工复验，自测不依赖用户真人代测（9e03b61 / 923ec6d）
