# 更新日志 2026-09-04

## ✨ 新功能 / 修复
- **feat(dial)**：转盘微调——目标集只收真实用户消息（`type==user_message` 且 source 非注入，复用 `isInjectedUserMessage`，注入/上下文消息不再被转盘定位）；`scrollToItem` 落底部后按 `scrollDeltaToTop(行高-视口高)` 补偿顶到屏幕顶部；圆钮刻度/红线放大更清晰（透明度 0.38 与 44dp 位置不变）；手势改单一恒挂 `DialSurface` + `awaitTouchSlopOrCancellation`，摁上不抬指直滑进旋转，未超阈值抬起 = 轻点展开，收起态命中半径外不消费手势不挡列表；`MessageDialMathTest` 19 例全绿（fbb2e14）
- **fix(app)**：Markdown 宽表格气泡内横向滑动——库默认固定列宽 160dp + Ellipsis 在单元格层就砍成「…」（库内部虽已 horizontalScroll 但只能滑被截断的固定宽表格）；经 `markdownComponents{table=}` 扩展点替换为最小复制版表格（单元格 softWrap=false/maxLines=1/overflow=Clip，TextMeasurer 按真实内容量列宽，外层 fillMaxWidth 锁气泡宽、内层 horizontalScroll）；横滑仅消费横向手势、竖向透传消息列表；新增 5 条单测（86f6c68）
- **fix(bridge)**：框架注入消息不入队列快照——根治子代理收尾消息 20s 循环重复送达：`queueItemsOf` 把 inbox 展示投影（含 subagent-settled/report/LSP 反馈等框架注入消息）直接用作持久化/恢复事实源，框架消息被消费后 `restoreQueueIfLost` 每 20s 巡检误判「重启丢失」→ 以 user 身份重注入（source.kind 由 subagent-settled 变 user）；改 `userQueueItemsOf`（仅 source.kind==='user'）作快照与恢复对比，注入循环加 placement==='context' 防御；与 cc35dc8 互补（时序 vs 归属）；smoke 加回归（970b477）

## 📝 文档
- **docs(bugs)**：远程（Tailscale）装机链路多次失败且无状态可观测性——可观测性缺失 / 交互式安装器假设 / 中断语义错位三条深层根因，进度监控与防误触待落地（9a8f9b1）
- **docs(bugs)**：重复送达 bug 文档回填 commit 哈希（b1499d7）
