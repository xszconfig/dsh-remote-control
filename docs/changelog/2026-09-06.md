# 更新日志 2026-09-06

> 决策落定日：九项决策记录固化、APK 瘦身实施（12.6MB→2.16MB）、中断确认弹框 + 发送状态(IM 模式) + goal 完成态隐藏、R4 DEX 硬门禁接入、历史决策文档回填 7 份。

## ✨ 新功能 / 决策落地
- **feat(app)**：中断确认弹框 + 消息发送状态(IM 模式) + goal 完成态隐藏——排队>0 时中断弹框二选一（终止并清空 / 仅终止保留 / 取消），`queuedCounts` 会话级计数，两个中断入口统一走新流程，ACTION 埋点；发送状态 `PendingMessages` 状态机（Sending/Sent/Failed）+ `PendingBubble`（时间行 Loading / 红色❗点击重发）+ 服务端回显去重（同会话同文本 60s FIFO）；`GoalPanel` 对 phase=complete 不渲染（对齐 DSH Web）；协议 Interrupt 增 mode(clear/keep 缺省 clear)，ProtocolTest 覆盖；客户端单测 114/114（8f0fa35）
- **feat(bridge)**：中断语义——interrupt 增 mode(clear/keep)：keep 模式快照 inbox 内 source.kind=user 消息全文 → cancel 清空 → 逐条 followup 重投（借 DSH 标准 followup 唤醒 idle agent 自动开新一轮消费，决策④「仅终止保留后自动续跑」）；clear 只 cancel 不重投；弃用 cancel({keepInbox:true})（只保留队列不唤醒，队列停在 idle 无人消费）；日志 INTERRUPT 打点 mode + 保留重投条数；smoke 3 条断言（7b23081）
- **feat(build)**：APK 瘦身——新增 release-in-house（R8 minify + 资源裁剪 + debug 签名 + arm64-only，内部测试装机）与 release-store（R8 + 资源裁剪 + arm64-only，商店包签名占位）；`proguard-rules.pro` 保留 serialization/zxing/mikepenz-markdown/org.intellij.markdown/入口 Activity（宁可多 keep 不误删）；debug 12.6MB → release-in-house 2.16MB（-83%，目标 ≤6.3MB）（df750a9）
- **chore(build)**：接入 R4 DEX registers_size 硬门禁——`checkDexRegisters` 挂 assemble 自动执行，扫 APK 内所有 classes*.dex 本包方法 registers_size，>256 报错（构建失败）、>128 告警（b8427dd）

## 🐛 修复
- **fix(interrupt)**：弹框正文用点按时刻的排队数快照，避免竞态显示「0 条」——渲染时重读 `queuedCounts[sessionId]`，若「点中断时队列非空 → 弹框出现 → 队列被消费」正文会显示「0 条排队消息」；改为点按瞬间快照 `confirmQueuedCount`（ceb5c05）
- **fix(app)**：发送状态语义修正——运行中仅排队不上屏，非运行中才走 PendingBubble 状态机（1eb461e）
- **fix(dial)**：扇面原地展开 + 收起图标改小半扇——展开扇面枢轴由「左缘中点」改「左缘、圆钮中心 Y」（`DIAL_PIVOT_BOTTOM_INSET_DP=38dp` = 10dp 底边距 + 28dp 圆钮半径），FanVisual 静态/旋转层 pivot 与 transformOrigin 同步，手势角度 pivot 按 phase 区分（slop 超阈值先按收起态 pivot 记角度再展开，按压直滑角度连续）；收起图标 5 条刻度按扇形张开（红基准线 0° + 左右各 2 短刻度 ±22.5°/±45° + 细弧线）（e629ebc）
- **fix(dial)**：七线半圆图标 + 翻页 loading 跟随扇面锚点——收起图标 5 线 → 7 线半圆（短刻度 ±22.5°/±45°/±67.5°），细弧线扩为 -67.5°~+67.5°（135°）勾勒半圆轮廓；WaitingOlder 的 spinner 由 CenterStart 改 BottomStart + padding(start 32, bottom 29)，垂直居中于枢轴 Y（29 = 38 - 9）；自查 MessageDial 内所有绝对位置元素（scrim 除外）已核对（1379985）
- **fix(dial)**：转盘改按需显示——圆钮复用 `showJumpToBottom`（latestMessageVisible/latestEventIndex 推导的「最新消息不可见」信号），上翻离开底部才出现、回底隐藏并收起；隐藏时若仍非收起态则 `LaunchedEffect` 调 `dialState.collapse()`（dialState 已 hoisted 在 Conversation 层，隐藏不销毁状态）（b0cb630）

## 📝 文档
- **docs(status)**：验收与决策状态台账建立（每日早报唯一事实源）（5c972b8）；每日早报 2026-09-06（50d6937）
- **docs**：九项决策记录固化——AGENTS.md 历史决策 + 状态台账同步（fef89bc / 04b0c79）；中断语义补全——仅终止保留后自动启动新一轮消费排队消息（bbcdf9b）；Kotlin LSP 选型定案——维持官方等修复、登记决策与已知限制（fe3914e）
- **docs(status)**：APK 瘦身已实施登记（bb19718）；LSP 更新结果登记——EAP 263.4421.0 已装、CCE 修复待上游发布（bfde6ae）；真机自测结果登记——9 PASS / 3 部分，ceb5c05 待装机复验（5d57ea0）
- **docs(decisions)**：历史重大技术决策回填 7 份（统一十节结构、全中文）——APK体积瘦身方案设计 / 调试协议选型设计 / KotlinLSP选型论证（原 kotlin-lsp-selection.md 重命名）/ 语言服务器六语言选型设计 / 秒编框架全景调研 / 自动续跑机制设计 / 桥插件热重载机制设计（5969f7a）；技术设计文档模板（十节结构、全中文规范）（96e5003）；Kotlin LSP 三实现对比论证——官方 / fwcd / Rust 版（20eb687）
- **docs(agents)**：铁律补强——铁律 1 强化（重大选型须先落 docs/decisions/ 对比论证再请用户拍板）（e8ed022）；铁律 13 交付标准（关联细节自查到位、禁止微操）（30a491b）+ 补强（设备级功能自测由实现子代理自己完成、单测是基线不替代）（b57c0fa）+ 措辞补强（对功能负责到底、不丢主对话、不依赖真人代测）（778f527）；铁律 14 语义冲突必二次确认（8b5cae1）；规则 11 补强（真机在线时自测一律优先在真机）（fec89ee）；bridge 侧同步（1acd08e / f4059e9 / 59563c4）

## 🎯 重要决策
- **九项决策（用户拍板）**：① APK 瘦身 = 两 release 变体 + arm64-only + ≤6.3MB；② Jugg 无头编译服务 MVP（2-5 人日）；③ Kotlin LSP 优先稳定版（含 LSP-1561 修复）；④ 中断语义 = 有排队消息时弹框二选一 + 「仅终止保留」后自动启动新一轮 Agent 循环消费；⑤ 发送状态 = IM 模式（点发送立即上屏 + 时间行 Loading / 失败红色❗点击重发 / 输入框立即清空）；⑥ 装机进度全程可见；⑦ R4 DEX registers_size 门禁挂构建自动执行（>128 告警 >256 失败）；⑧ goal 完成态对齐 DSH Web 隐藏；⑨ GitHub 推送暂不推（等全部验收后再说）

## ⏳ 待办
- 真机自测 9 PASS / 3 部分，ceb5c05（中断弹框快照）待装机复验；GitHub 推送等用户明确指示
