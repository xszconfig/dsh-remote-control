# 更新日志 2026-08-30

> IDE 级能力全天冲刺：bridge 0.10.3 → **0.11.9**，冒烟回归 48 → 76 断言。详细叙述见 `../progress-2026-08-30.md`。

## ✨ 新功能（按域）

**LSP 代码智能（6 语言）**
- LSP 首版（编辑文件 → 诊断广播手机）→ 真机打通 → 会话隔离
- 官方 JetBrains kotlin-lsp 接入（IntelliJ 内核，六层 0 诊断根因修复）
- lsp_query Agent 工具（diagnostics/hover/definition/references，对齐 oh-my-pi）

**Debug 受控调试（CDP / Node Inspector）**
- 断点/暂停调用栈/变量惰性展开/单步/跳出/输出流全链路
- 手机调试面板（开发状态行聚合徽标 → 抽屉：诊断/调试 Tab）
- debug_start/debug_command Agent 工具（0.11.9）

**Goal / 任务列表**
- Goal 面板（goal/change 投影广播 + 订阅携带；cordis inject 陷阱修复）
- 任务列表（todos 投影 + turn/start 空清单推送；手机条 ≤3 滑动）

**Deep Diving 服务端权威计时**
- deep_diving_tick 每秒广播 → 对齐 DSH Web（turn_status 全轮次标签 + ≥15s 时钟 + turnSince 订阅携带 + 秒/分/时格式）

**排队 / 自动续跑**
- 排队面板 ≤3 条滑动；跨重启快照恢复（work.json queues + 去重幂等）
- 自动续跑 v1（boot 延迟注入）→ 根治版（20s 持续重试 + 指纹幂等 + sessionId 归属 + agent/status 触发）

**UI 细节**
- 发送按钮 → Agent 运行中变红色终止按钮；顶栏去「断开」+ 标题加宽
- Goal 折叠单行；任务列表条（Deep Diving 下、Goal 上）

**其它**
- Code Diff（红删绿增）、Markdown 渲染（mikepenz）、思考流 100ms 节流、服务端重启通知横幅（server_boot）

## 🐛 修复（代表性）
- inbox/spliced 滞后一拍（queueMicrotask 延迟广播）
- 历史按投影行翻页（109 万事件大会话不卡死）；投影顺序翻回旧→新
- Kotlin 0 诊断六层根因（pull 模式/content-root 门控/initialized 通知化/version 溢出/realpath 软链/ready 时序）
- goal/todos 属性直读触发 cordis inject 陷阱 → ctx.get 软读
- 调试注册表残留（"已有调试进程在运行"卡死）
- 自动续跑"暂无 live agent 就永久放弃" → 持续重试

## 🎯 重要技术决策（全部经用户拍板）
1. **Kotlin LSP 选型**：官方 JetBrains kotlin-lsp（brew tap/vsix 解包）而非 fwcd 社区版
2. **调试协议选型**：CDP（Node Inspector 直连）而非 DAP（OMP 路线）——论证文档 `debug-protocol-cdp-vs-dap.md`，DAP seam 预留
3. **协作铁律写入两仓库 AGENTS.md**：重大架构/路线/选型必须先问用户等 8 条
4. **服务端权威原则强化**：计时/任务列表全部以服务端投影为准，客户端删除本地推算

## 📊 质量数据
- 48h 编译统计：App 89 次/26 错（29.2%，最高频 Unresolved reference）；Bridge 85 次/13 错（15.3%，最高频 TS2322 类型族）
- 冒烟 76/76；debug 真机同路径 8/8；真机交互验收（排队 5 条、任务列表 5 条）

## ⏳ 遗留（转 08-31）
- 自动续跑速度优化（目标挂载后 ≤3s 唤醒）
- 手机视觉验收：Deep Diving 标签全轮次 + 15s 时钟 + 任务列表轮次清空
