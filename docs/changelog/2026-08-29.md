# 更新日志 2026-08-29

> 工作量大的一天：客户端架构重构 + 消息底座（排队/历史/日志/重连）全面成型。详细叙述见 `../progress-2026-08-29.md`。

## ✨ 新功能（bridge）
- 会话列表合并持久化层（冷会话可见、标题折叠、工作区归属、倒序）+ 冷会话订阅历史 + 会话 created/disposed 增量事件 + 冷会话发送友好报错
- 排队消息透传：subscribe 带 inbox 队列、spliced 广播 session_queue、插队(steer)/删除(remove)
- 历史分页：subscribe 尾部 300 条 + history_page 按 seq 翻页（冷会话缓存 5 分钟）
- 手机日志回传：/remote/phone-logs 广播 logs_request → 手机 upload_logs
- 子代理：会话行携带 parentSessionId
- 事件增强：工具调用带桌面端描述、Think 步骤单独投影、callId 失败标红、llm/stream → model_waiting/done（Deep Diving 指示）
- 多路由重连：设备注册下发候选端点

## ✨ 新功能（app）
- 断线自动重连（指数退避 + 会话数据保留 + 审批不丢）；冷启动检测上次设备自动连接；设备页（切换/扫码/手动连接）
- 会话/工作区本地缓存秒开 + Hello 增量对账；历史分页加载（上滑自动加载更早页）
- 排队消息面板（乐观入队、插队/删除）；消息自动跟随最新；输入框草稿持久化（防抖 600ms）
- 子代理会话挂载主会话（🤖 下拉入口 + 返回键回主会话）
- Deep Diving 指示条（初版，客户端本地计时）
- 失败工具调用标红；工具调用/Think 单行卡片（对齐 DSH Web）

## 🐛 修复
- 真机连接必败：快速失败 + 逐候选诊断 + USB 引导
- 设备持久化并发丢写竞态：原子写 + 写放大节流
- 长期 token 改 Authorization Bearer 头（不再明文拼 URL）
- 断开不再清空用户偏好；返回导航改纯本地（消除 openSession("") hack）
- 指令发送失败不再静默丢弃（错误提示封顶 UI 可见）
- 会话事件 500 条截断（杜绝无界内存增长）
- 工具结果卡片展示真实输出（嵌套提取）
- 全局对比度修复（MaterialTheme 默认纯黑导致深底黑字）
- 发送按钮 48dp 圆钮内边距归零（纸飞机图标被挤没）
- 日志页连接态可达 + 服务端日志回退最近在线设备
- 重连横幅清除改由 hello 权威信号驱动；断线永不跳页（横幅多路由选择）
- 冷会话列表改读投影缓存（修复大日志 JSON 解析阻塞事件循环导致服务卡死）
- inbox/spliced 打点 + 容错（队列投影异常曾静默吞广播）

## 🏗 架构
- **BridgeClient 上帝对象拆三层**；UI 状态流按界面拆分
- Pairing/URL 解析提取为纯函数 + 测试；ApprovalDecision 枚举替换魔法字符串
- 连接层结构化日志基础组件（本机 + 服务端双视图，两端全链路打点）

## 🎯 重要决策与规则沉淀
- 工作流 Memory 系列：只本地 commit（推送等用户指示）、重启前经用户确认、重启脚本保留完整 PATH、禁止非预期页面跳转、编译→自动安装→自动验收、简单检查一条命令收尾
- UI 对比度规范 + 页面层级/返回键规范 Memory
