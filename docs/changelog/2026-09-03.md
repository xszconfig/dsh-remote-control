# 更新日志 2026-09-03

> 全天修复冲刺：消息转盘 v1 落地；连接状态提示统一、消息来源分类、跨端契约漂移、鉴权缺口、热重载依赖解析等一批 P0/P1 修复；并建立疑难 bug 经验库（docs/bugs/）。

## ✨ 新功能
- **feat(dial)**：消息转盘 v1——右侧扇形拨轮快速定位用户消息：收起 44dp 半透明圆钮（右缘中段），点击展开 90° 四分扇（半径 128dp，红线在角平分线）；一格 18° = 一条用户消息 = 一步进，顺时针 = 更新、逆时针 = 更老，直接 `scrollToItem` 无动画；卡住语义（loadingOlder 单飞守卫 + 翻转唤醒 + 转动意图窗口/10 页上限）；每步一振 60ms 合并（`platformVibrateTick`，createOneShot(15,96)/触底(30,128)）；纯函数 `MessageDialMath`（索引映射/角度→步进/方向归一化/状态机/节流）独立可测；角度走 `mutableFloatStateOf` + `graphicsLayer{rotationZ}` 只读不重组；不改 bridge/BridgeClient（db07b72）

## 🐛 修复
- **fix(app)**：子代理下拉 intrinsic 崩溃——material3 DropdownMenu 以 `width(IntrinsicSize.Max)` 测内容区，LazyColumn 是 SubcomposeLayout → 打开会话页 🤖N 子代理下拉必现 `IllegalStateException`；换 `Column(heightIn(max).verticalScroll)`，保留 10 条上限与滚动语义；全仓库排查无其它 DropdownMenu/IntrinsicSize 组合（9968939）
- **fix(app)**：统一连接状态提示槽，根治错误提示过期残留——`ConnectionNotice` sealed 状态机（Hidden/Reconnecting(attempt)/Error）取代 `_reconnecting` 布尔；`errors` 改 `List<NoticeError>(message,recoverable)`，`hello` 到达清可恢复错误、保留业务错误；App 端 `ConnectionErrorBanner` + `ErrorHistorySheet` 底部 sheet（保留「还有 N 条」+ 展开历史）；重连中抑制连接类错误堆积（1fd1ab6）
- **fix(app)**：消除重连入口重断言与 hello 的竞态，根治横幅残留——真机 USB 隧道恢复后 hello 已到达但横幅约 1 分钟才消失；`startReconnect` 入口不再重断言，`beginReconnectNotice` 只由重连循环内 open() 前调用（连接存续期间 open() 阻塞、无挂起重试定时器，hello 清槽后不会被再次置位）；handle 改 internal + 新增单测（06acd62）
- **fix(app)**：连接失败时撤销 hint 残留的 connectedDevice——失败分支 `clearConnectedDeviceIf(key)`（仅当残留仍属该设备才清），两字段同源；顺带修正 `keepSessionUi` 误判引起的非预期跳页；新增 3 条单测（f704fa5）
- **fix(app)** + **fix(bridge)**：user_message 按 source 区分用户气泡与上下文注入行——协议增 `source` 字段（app `EventProjection.source` / bridge `source?: 'user'|'inject'`，bridge 按 DSH `UserMessage.source.kind` 权威分类：kind==='user'→'user'，其余→'inject'）；注入/上下文消息不再落回用户气泡，渲染为弱化 `ContextRow`「上下文」折叠行；app `isInjectedUserMessage` 前向安全（未知非 user 值不落回用户）；smoke 4 例 + app 4 例单测（fd4a451 / 2205778）
- **fix(protocol)**：Kotlin Hello 补 TS EvHello 的 lsp/work 字段（S1 契约漂移）——新增 `HelloLsp(languages)`/`HelloWork(activity,pending)` 具名类，Hello 增 lsp/work 可空字段（旧版 bridge 缺省 null）；`ProtocolTest` 增 decode 断言（645ccbe）
- **test(bridge)**：跨端 wire schema 契约检查（止血 S1 手写双份漂移）——`test/contract-check.mjs` 解析 TS interface 字段集合与 App `Protocol.kt` 的 `@Serializable` data class 字段集合比对，有意不对称经 TS_ONLY/KT_ONLY/NAME_MAP 显式 allowlist 记录；挂入 pnpm test，当前唯一漂移（Hello lsp/work）由 645ccbe 补齐（c9c7103）
- **fix(bridge)**：WS 匿名 open 对齐 REST 回环校验（S1 鉴权缺口）——无 envToken 且无任何候选凭证时不再无条件 open，要求 `isLoopback(remoteAddress)&&isLoopbackHostHeader(host)` 才放行，否则 401；堵住 Tailscale serve/SSH -L 转发远程流量到 127.0.0.1 的匿名放行；smoke 两条 401 断言（6dd4009）
- **fix(bridge)**：热重载首次激活依赖解析失败——`findNodeModules` 只取最近一层 node_modules，pnpm 分层使 @deepseek-ai/* 在远层（profiles/node_modules）而近层缺失 → 首次激活 core.js import 抛 ERR_MODULE_NOT_FOUND（生产实锤 0.13.0）；改 `collectNodeModules` 收集全部祖先层、近→远合并顶层条目（近层优先）、单条目 symlink 失败只跳过；新增两层 node_modules 回归（6d5da59）

## 📝 文档
- 疑难 bug 经验库建立：app 仓库新增 `docs/bugs/` 模板 + 重连横幅滞留、子代理下拉 intrinsic、提示条过期残留三份文档（c69669e / 31e85da / fc92f76 / d9bd910）；bridge 仓库同步新增模板 + 队列快照防抖、热插拔依赖解析、消息来源分类文档（f56c823 / db96ddc）；两仓库 AGENTS.md 增「疑难 bug 必存文档」铁律（app 铁律 12 / bridge 铁律 13）

## ⏳ 待办
- 转盘 v1 待真机验收；消息来源分类、连接提示槽、契约检查待装机复验
