# 更新日志 2026-09-05

> 全天冲刺：转盘改版（左移 + 五线图标 + 方向语义调转）、DEX VerifyError 启动崩溃根治（拆巨型 Composable）、lint 体系（detekt/ESLint）接入、时间戳 IM 规则、行为日志埋点。

## ✨ 新功能
- **feat(dial)**：转盘改版——挂载点左移到消息列表 Box 内（LazyColumn 与 JumpToBottomOverlay 的父容器），圆钮悬浮列表左下角 = Deep Diving 上方，队列/任务/Goal 面板上推 Deep Diving 时圆钮随之移动；左缘开口扇面（枢轴右缘→左缘、startAngle 135°→-45°、红基准线 180°→0°、transformOrigin 镜像，角度符号不变）；五线小转盘图标（44dp→56dp，红基准线 0° 最长最粗 + 左右各 2 短刻度，KNOB_HIT_RADIUS 28→34，收起态手势表面 52dp→56dp 与圆钮等大）（0543a32）
- **feat(app)**：客户端行为日志埋点——发送/中断/输入/列表加载翻页全覆盖（42740d5）
- **feat(app)**：时间戳展示统一 IM 规则——今天/昨天/前天/跨年映射全覆盖（6ea70b4）；时间戳恒显 + 日期前缀——今天/昨天/前天/M月d日/跨年（688257e）

## 🐛 修复
- **fix(app)**：拆分 Conversation 巨型 Composable——根治 DEX 校验器 VerifyError 启动崩溃（590 行 Composable 267+ 寄存器，copy1 4-bit 操作数非法编码，debug 无 R8 直达设备；拆 9 函数）（249bb4a）
- **fix(dial)**：收起态手势表面缩小至圆钮区——根治转盘条吞掉发送/中断按钮点击的 P00 缺陷：Compose 命中测试对带 pointerInput 的兄弟节点顶层独占（「未命中不消费」不能把事件交给底层兄弟）；收起态表面 128dp 全高 → 52dp 见方（44dp 圆钮 + 8dp 右边距），命中常量改绝对 dp，pivot 随 size 现取现算（收起 52dp→展开 128dp 角度连续）（ab091b9）
- **fix(dial)**：新消息不再打断转盘交互——转盘展开/转动定位旧消息时 `LaunchedEffect(latestSeq)` 仍 `scrollToItem(0)` 强制滚底打断交互；`MessageDialState` 提升到 Conversation 层（remember(state.currentSessionId)），滚底 effect 读 `dialState.phase != Collapsed` 时跳过；转盘收起后恢复正常跟随，followBottom 状态机不改（868b547）
- **feat(dial)**：转盘方向语义调转——stepResult 的 delta→dir 映射符号调转（delta +1 由「更新」改「更老」，target = selectedIndex - delta），顺时针 = 看更老、逆时针 = 看更新；`MessageDialMathTest` 方向断言修正（b4af84e）
- **fix(bridge)**：Agent 工具调用强制超时——lsp_query/debug 工具未声明 timeoutMs 故 DSH 超时策略不生效，且 `LspManager.request()`/`InspectorSession.send()` 在进程退出/断开时只 `pending.clear()` 不 reject，在途 Promise 永不落定 → await 无限挂起（lsp_query 曾卡死 agent 回合 400+ 分钟）；query() 内 10s deadline 双保险 + 死进程快速失败（exitCode/signalCode/killed）+ pending 改可 reject；三工具声明 timeoutMs:10_000；新增共享 `withTimeout` 助手；pnpm test 132/132（77e4146）

## 🛠 重构
- **refactor(app)**：拆分 `BridgeClient.handle` 巨型 when——按事件类型下沉为 29 个 handler（b2737e6）；拆分 LandingScreen/审批/提问/日志/调试面板 5 个巨型 Composable（72a04be）；拆分 MessageDial 巨型 Composable——下沉交互面到 DialOverlay（703c2d0）

## 🎯 质量闸门（lint 体系）
- **chore(lint)**：app 接入 detekt 代码质量闸门（P0 子集 + 全量 + pre-commit 拦截）（a312a99）；bridge 接入 ESLint 代码质量闸门（P0 子集 + 全量 + pre-commit 拦截）（ee3ffbf）

## 📝 文档
- **docs(bugs)**：App 启动即崩 VerifyError 案例（Leo 补记）（4d75aec）；转盘恒挂手势条吞点击案例——补模拟器验收结论（45d32e8）
- **docs(coding-rules)**：VerifyError 源码级深挖——三层机制 / 14 条全景表 / 6 条编码规则（2fa928d）
- **docs(agents)**：验收流程第一步固化为启动冒烟 P00 硬断言 + 登记 DEX 校验器防回归规则（833b4b3）
- **docs**：功能盘点与优先级地图——13 条 P0 + 6 条核心价值挖掘（5a6465e）
- **docs(bugs)**（bridge）：Agent 工具无超时挂死事故（lsp_query 400+ 分钟）（8b32f82）

## ✅ 真机 / 模拟器验收
- 模拟器验收通过：启动无崩 / 发送点击链路 / 排队移除 / 转盘左移改版（点开-拨动-边界翻页-自动收起）/ 日志页；中断按钮以 dump 位置确认无覆盖层；同批验证 Conversation 拆分修复（45d32e8 记录）
- VerifyError 修复版已装机验收（2026-09-05 10:46）

## ⏳ 待办
- 时间戳新格式、转盘改版、方向调转、新消息不打断转盘、行为日志、宽表格横滑、回到底部、子代理元信息等 8 项待真机验收（转 09-06 早报）
