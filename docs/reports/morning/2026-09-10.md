# 每日早报 · 2026年9月10日

## 一、待人工验收

- **APK 瘦身**（`df750a9`）——新增 release-in-house + release-store 两 buildType（R8 + arm64-only），release-in-house 2.16MB（debug 12.6MB → −83%，目标 ≤6.3MB）；模拟器冒烟 + 连接/hello/序列化/日志页 tab/问题弹层回归通过；待真机装机验收。
- **时间戳新格式**（`688257e`）——前缀 + 时刻恒显（今天/昨天/前天/M月d日/跨年），单测 22 例 + 模拟器 UI 已验证；待真机装机后验收。
- **转盘改版**（`0543a32`）——左移 Deep Diving 上方 + 56dp 五线小转盘图标，模拟器验证；待真机目验位置/图标。
- **转盘方向调转**（`b4af84e`）——顺时针=更老 / 逆时针=更新，模拟器日志证据验证；待真机手感验收。
- **新消息不打断转盘**（`868b547`）——非收起态挂起强制滚底，门控代码验证；待真机实测（自动化实测受会话事件流干扰）。
- **行为日志埋点全覆盖**（`42740d5`）——发送/中断/输入/列表/翻页/转盘/日志页，日志证据验证；待抽查。
- **Markdown 宽表格横滑**（`86f6c68`）——5 条单测；待人工目验（此前锁屏未复验）。
- **跳到底部按钮回归**（`4184b59`）——待目验。
- **子代理列表元信息 + 滚动**（`fc1dfe19`）——待目验。

## 二、做完未自测

- **主动通知与审批提醒（本地通知 v1）**（`9d0a4c6`）——三态门控（前台浏览该会话不发/前台其它会话/后台锁屏发）+ 审批/提问高优幂等（approvalId/rpcId）+ 结果交付 D1/D2/D3（会话×轮次幂等）+ 夜间勿扰 22:00–08:00 仅静默结果交付 + 不合并；单测 20 例 + detektP0 全绿；⏳ 待设备自测（模拟器：审批/提问后台通知、浏览态抑制、结果交付、去重/勿扰）。

## 三、调研完待决策

1. **Jugg 无头编译服务 MVP**——✅ 已决策：实施 MVP（2-5 人日）；⏳ 子代理实施中（先设计后实现）。
2. **Kotlin LSP**——✅ 已定案（2026-09-06）：维持官方，待上游发布含 LSP-1561 修复的构建后按 UPGRADE-NOTES 升级（fwcd 已 deprecated、Rust 版无类型检查均不迁）。
3. **中断语义**——✅ 已决策：有排队消息时弹框二选一（终止并清空 / 仅终止保留，加取消），「仅终止保留」后队列仍有余量时自动启动新一轮 Agent 循环消费；⏳ 子代理实施中。
4. **发送状态（IM 模式）**——✅ 已决策：点发送立即上屏 + 同时间行 Loading、成功消失、失败红色❗点击重发、输入框立即清空；⏳ 子代理实施中。
5. **装机进度显示**——✅ 已决策：全程可见（包大小/传输百分比/步骤/错误归类）；⏳ 子代理实施中。
6. **R4 规则接入 lint**——✅ 已实施：checkDexRegisters 任务挂 assemble 自动执行（>128 告警 >256 失败），buildSrc 纯函数解析器 5 例单测。
7. **goal 完成态隐藏**——✅ 已决策：对齐 DSH Web 隐藏；⏳ 排队实施中。
8. **推 GitHub**——✅ 已决策：暂不推（等全部验收后再定）。
9. **TS core.ts 3 处 lint 豁免拆分**（apply/handleCommand/projectEvent）——收紧路径已登记 `docs/lint-rules.md`。

## 四、昨日工作衔接

### 仓库提交

- **app**（2026-09-09 08:12 ~ 09-10 08:12，17 条）：
  - 输入区操作条 App UI 改版 `b409bb9`
  - 排队面板默认折叠 + 两 bug 修复（`4797403` 默认折叠、`0e430d5` 只显示用户消息 + 顺序对齐服务端投影、`a26f091` 合并；`66cc772` 5-Why 复盘文档）
  - 转盘收起钮与跳到底按钮视觉对齐（48dp/9 刻度/底距，`d3ece34`、`6752e4c`）
  - 结果交付通知 App 侧方案③消费——补投递闭环 `c97451a`
  - 按需前台服务保活 + 白名单引导（方案②+⑤）`084256b`
  - 浅色主题修正 + 设置入口从 TopBar 移到侧边栏底部（`317305b`、`00faa66`、`4844460`、`24ca7de`、`679c852`）
  - Deep Diving 时长文案去掉「本轮」`8247176`
  - 文档类：验收规则入 AGENTS.md + 截图留档 + 验收报告落盘回填（`616215a`、`c172482`、`f040325`）
- **bridge**（2 条）：
  - 输入区操作条数据面——模型目录/上下文占用推送 + set_model 路由（v0.16.0）`9ca7d73`
  - 结果交付通知服务端补投递——turnKey 服务端权威 + 台账持久化 + 重连补发 + 确认（v0.15.0）`4818bb8`
- **服务端 health**：`{"ok":true,"version":"0.16.0","sessions":89,"work":{"activity":"0.12.0 激活与真机验收","pending":4}}`

### 新增 bug 文档

- `2026-09-10-queue-panel-non-user-items-and-order.md`——排队面板混入注入消息 + 乐观项顺序不一致
- `theme-light-incomplete.md`——浅色模式深色残留
- `2026-09-07-steer-queue-item-not-found-message-vanished.md`——队列项缺失致消息消失
- `2026-09-07-pending-message-lost-on-session-exit.md`——会话退出丢待发消息
- `2026-09-06-dial-missing-in-subagent-sessions.md`——子代理会话转盘缺失

### 今日待办提醒

- **真机安装提醒**：手机 adb 通道（Tailscale/USB）恢复后安装 20:55 最新包（含时间戳新格式 + 转盘两改版），随后按 A 节逐项人工验收。
- 新工具就位：`startup-smoke-test` skill（P00 启动冒烟，存活 ≥5s 硬断言）、`code-lint` skill + pre-commit P0 闸门，已写入 AGENTS.md；工具调用超时修复（lsp_query/debug 10s deadline）已热重载上线。
- 决策台账：C 节 1/3/4/5/7 项均「已决策待实施」，推动子代理实施清零；Kotlin LSP 监控上游 LSP-1561 修复发布。
- 服务端 work.pending = 4，注意队列消化。

## 今日建议优先处理

1. **发送状态（IM 模式）**——已决策（立即上屏 + Loading + 失败重发），子代理实施中；核心消息体验，且有此前「既上屏又排队双份显示」返工教训，优先落地并自测。
2. **装机进度显示**——已决策（全程可见），子代理实施中；真机安装是当前验收链路的卡点，进度可见性能直接支撑装机环节。
3. **中断语义**——已决策（弹框二选一 + 续跑），子代理实施中；与 A 节多条待验收项联动，且此前中断弹框竞态修复（`ceb5c05`）已进最新 APK 待装机复验。
