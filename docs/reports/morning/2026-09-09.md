# 每日早报 · 2026年9月9日

## 一、待人工验收
- APK 瘦身（release-in-house + release-store 开 R8 + arm64-only）—— `df750a9`：release-in-house 已缩至 2.16MB（debug 12.6MB → −83%），模拟器冒烟与回归通过，待真机装机验收。
- 时间戳新格式（前缀 + 时刻恒显：今天/昨天/前天/M月d日/跨年）—— `688257e`：单测 22 例 + 模拟器 UI 已验证，待真机装机后验收。
- 转盘改版（左移 Deep Diving 上方 + 56dp 五线小转盘图标）—— `0543a32`：模拟器验证，待真机目验位置/图标。
- 转盘方向调转（顺时针=更老 / 逆时针=更新）—— `b4af84e`：模拟器日志证据验证，待真机手感验收。
- 新消息不打断转盘（非收起态挂起强制滚底）—— `868b547`：门控代码验证，待真机实测（自动化实测受会话事件流干扰）。
- 行为日志埋点全覆盖（发送/中断/输入/列表/翻页/转盘/日志页）—— `42740d5`：日志证据验证，待抽查。
- Markdown 宽表格横滑 —— `86f6c68`：5 条单测，待人工目验（此前锁屏未复验）。
- 跳到底部按钮回归 —— `4184b59`：待目验。
- 子代理列表元信息 + 滚动 —— `fc1dfe19`：待目验。

## 二、做完未自测
- 主动通知与审批提醒（本地通知 v1）—— `9d0a4c6`：三态门控（前台浏览该会话不发/前台其它会话/后台锁屏发）+ 审批/提问高优幂等 + 结果交付幂等 + 夜间勿扰 22:00–08:00；单测 20 例 + detektP0 全绿；⏳ 待设备自测（模拟器：审批/提问后台通知、浏览态抑制、结果交付、去重/勿扰）。

## 三、调研完待决策
- Jugg 无头编译服务 MVP——✅ 已决策：实施 MVP（2-5 人日）；⏳ 子代理实施中（先设计后实现）。
- Kotlin LSP——✅ 已定案（2026-09-06）：维持官方版本，等 LSP-1561 修复构建发布后按 UPGRADE-NOTES 升级（论证见 docs/decisions/kotlin-lsp-selection.md）。
- 中断语义——✅ 已决策：有排队消息时弹框二选一（终止并清空 / 仅终止保留+自动续跑新一轮消费）；⏳ 子代理实施中。
- 发送状态（IM 模式）——✅ 已决策：点发送立即上屏 + 同行 Loading，成功消失、失败红❗点击重发；⏳ 子代理实施中。
- 装机进度显示——✅ 已决策：全程可见（包大小/传输百分比/步骤/错误归类）；⏳ 子代理实施中。
- R4 规则接入 lint——✅ 已实施：checkDexRegisters 挂 assemble 自动执行（>128 告警 >256 失败），buildSrc 纯函数解析器 5 例单测。
- goal 完成态隐藏——✅ 已决策：对齐 DSH Web 隐藏；⏳ 排队实施中。
- 推 GitHub——✅ 已决策：暂不推（等全部验收后再定）；台账中另有一行重复条目「等待用户明确指示」，以已决策口径为准。
- TS core.ts 3 处 lint 豁免拆分（apply/handleCommand/projectEvent）——收紧路径已登记 docs/lint-rules.md。

## 四、昨日工作衔接
### 仓库提交（2026-09-08 08:12 ~ 2026-09-09 08:12）
- **app**（26 条）：消息发送鲁棒性阶段1客户端——msgId+ack+断线自动重放+应用层判活（`2e7bc3b`/`860dbd6`/`2b23541`）；主题三态 + 颜色盘 token 化、硬编码色清零（`3286a7e`/`96cd80c`）；Markdown 代码块行号 + 长行不换行 + 横向滑动（`5df6467`/`e2c4fc6`/`d8cc141`）；平板三栏适配（断点判定/左栏复用/三栏布局，`9861e3e`/`6ef15c4`/`df60a2d`）；待发送消息持久化（`a7685ec`/`46499ef`）；通知收尾——点击直达会话 + 权限引导（`8ee7133`）；转盘修复×2（子代理会话不生效 `5b7f933`；旋转中不因到底隐藏 `07a8837`）；排队插队「已不在队列」修复（`a7f835b`/`c42454c`）；决策文档（`d969542`/`bba6fb7`）；每日早报 2026-09-08（`13cd560`）。
- **bridge**（2 条）：消息必达底座 v0.14.0——send_message 增 msgId+ack+幂等 + 应用层 ping/pong 判活 + LAN 二级 WS 监听（`c379bb0`）；coordinator source.kind 判为用户——修复子代理会话转盘定位（`8ac3e39`）。

### 新增 bug 文档
- `2026-09-06-dial-missing-in-subagent-sessions.md`：消息转盘在子代理会话不生效。
- `2026-09-07-markdown-table-needs-blank-line.md`：Markdown 表格需空行。
- `2026-09-07-pending-message-lost-on-session-exit.md`：待发送消息丢失 5-Why 复盘（pending 持久化修复）。
- `2026-09-07-steer-queue-item-not-found-message-vanished.md`：排队插队报「已不在队列」且消息看似消失。

### 今日待办提醒
- **真机安装提醒**：手机 adb 通道（Tailscale/USB）恢复后安装 20:55 最新包（含时间戳新格式 + 转盘两改版），随后对 A 节 9 项做真机逐项复核。
- 知晓即可（D 节新增，自测完无需验收）：`startup-smoke-test` skill、`code-lint` skill + pre-commit P0 闸门、`docs/coding-rules/verifyerror-deep-dive.md`（14 条 VerifyError 全景 + R1~R6）、`docs/feature-priority-map.md`（13 条 P0 功能地图）、工具调用超时修复（lsp_query/debug 10s deadline）。
- 服务端 health：ok（bridge v0.14.0，sessions 84，work.pending 4）。

## 今日建议优先处理
1. Jugg 无头编译服务 MVP——已决策实施（2-5 人日），子代理实施中，按「先设计后实现」推进（C 节第 1 项）。
2. 中断语义——已决策弹框二选一（终止并清空 / 仅终止保留 + 自动续跑），子代理实施中（C 节第 3 项）。
3. 发送状态（IM 模式）——已决策立即上屏 + Loading + 失败红❗重发，子代理实施中（C 节第 4 项）。
