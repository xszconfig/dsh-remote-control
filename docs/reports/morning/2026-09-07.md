# 每日早报 · 2026年9月7日

## 一、待人工验收

> 2026-09-06 真机全面自测（HBN-AL00）：11 项中 9 项 PASS、3 项部分通过（新消息不打断转盘/宽表横滑/排队删除动作为时机依赖项，留待用户自然验收）；期间发现并修复中断弹框竞态 bug `ceb5c05`（点按快照排队数），该修复已进最新 APK、待手机解锁后装机复验。

- [ ] **APK 瘦身**（新增 release-in-house + release-store 两 buildType 开 R8 + arm64-only）—— `df750a9`，release-in-house 2.16MB（debug 12.6MB → −83%，目标 ≤6.3MB）；模拟器冒烟 PASS + 连接/hello/序列化/日志页 tab/问题弹层回归通过；待真机装机验收
- [ ] **时间戳新格式**（前缀+时刻恒显：今天/昨天/前天/M月d日/跨年）—— `688257e`，单测 22 例 + 模拟器 UI 已验证；待真机装机后验收
- [ ] **转盘改版**：左移 Deep Diving 上方 + 56dp 五线小转盘图标 —— `0543a32`，模拟器验证；待真机目验位置/图标
- [ ] **转盘方向调转**（顺时针=更老 / 逆时针=更新）—— `b4af84e`，模拟器日志证据验证；待真机手感验收
- [ ] **新消息不打断转盘**（非收起态挂起强制滚底）—— `868b547`，门控代码验证；待真机实测（自动化实测受会话事件流干扰）
- [ ] **行为日志埋点全覆盖**（发送/中断/输入/列表/翻页/转盘/日志页）—— `42740d5`，日志证据验证；待抽查
- [ ] **Markdown 宽表格横滑** —— `86f6c68`，5 条单测；待人工目验（此前锁屏未复验）
- [ ] **跳到底部按钮回归** —— `4184b59`；待目验
- [ ] **子代理列表元信息 + 滚动** —— `fc1dfe19`；待目验

## 二、做完未自测

- （暂无）

## 三、调研完待决策

> 原第 1 项「APK 瘦身」已于 `df750a9` 实施（两 buildType 开 R8 + arm64-only），见 A 节。

1. **Jugg 无头编译服务 MVP**——✅ 已决策：实施 MVP（2-5 人日）；⏳ 子代理实施中（先设计后实现）
2. **Kotlin LSP**——✅ 已定案（2026-09-06）：维持官方，待上游发布 LSP-1561 修复构建后按 UPGRADE-NOTES 升级（三实现对比论证见 docs/decisions/kotlin-lsp-selection.md）
3. **中断语义**——✅ 已决策：有排队消息时弹框二选一（终止并清空 / 仅终止保留，加取消）；「仅终止保留」后队列仍有待执行消息时自动启动新一轮 Agent 循环消费；⏳ 子代理实施中
4. **发送状态（IM 模式）**——✅ 已决策：点发送立即上屏+同时间行 Loading；成功消失；失败红色❗点击重发；输入框立即清空；⏳ 子代理实施中
5. **装机进度显示**——✅ 已决策：全程可见（包大小/传输百分比/步骤/错误归类）；⏳ 子代理实施中
6. **R4 规则接入 lint**——✅ 已实施：checkDexRegisters 任务挂 assemble 自动执行（>128 告警 >256 失败），buildSrc 纯函数解析器 5 例单测
7. **goal 完成态隐藏**——✅ 已决策：对齐 DSH Web 隐藏；⏳ 排队实施中
8. **推 GitHub**——✅ 已决策：暂不推（等全部验收后再定）；另登记待办：等待用户明确指示
9. **TS core.ts 3 处 lint 豁免拆分**（apply/handleCommand/projectEvent）——收紧路径已登记 `docs/lint-rules.md`

## 四、昨日工作衔接

### 仓库提交

- **app**（2026-09-06 08:12 → 2026-09-07 08:12，共 31 条）：
  - 功能：连接后自动打开最近会话（`13baa10`）；中断确认弹框 + IM 发送状态 + goal 完成态隐藏（`8f0fa35`）；发送状态语义修正——运行中仅排队不上屏（`1eb461e`）
  - 修复：中断弹框排队数竞态快照（`ceb5c05`）；转盘按需显示（`b0cb630`）、七线半圆图标 + 翻页 loading 跟随扇面锚点（`1379985`）、扇面原地展开 + 收起图标改小（`e629ebc`）
  - 构建：R4 DEX registers_size 硬门禁接入 assemble（`b8427dd`）；APK 瘦身两 R8 变体（`df750a9`）
  - 文档/流程：主流 Coding Agent 竞品调研 12 产品（`50040ac`）；changelog 回填六篇（`86bac64`）；历史决策回填 7 份（`5969f7a`）；Kotlin LSP 选型定案（`fe3914e`）；AGENTS.md 铁律 1/11/13/14 补强多条；台账同步（真机自测登记 `5d57ea0` 等）；早报 2026-09-06（`50d6937`）
- **bridge**（共 4 条）：
  - `7b23081` feat: 中断语义——interrupt 增加 mode(clear/keep)，keep 快照用户队列→cancel→followup 重投自动续跑消费
  - `1acd08e` / `f4059e9` / `59563c4` docs: 铁律同步（设备级自测由实现子代理完成 / 语义冲突必二次确认 / 交付标准禁止微操）

### 新增 bug 文档

- `docs/bugs/2026-09-05-dial-surface-swallows-send-interrupt.md`——转盘表面吞掉发送/中断手势问题
- `docs/bugs/2026-09-05-verifyerror-startup-crash.md`——VerifyError 启动崩溃（R1~R6 规则沉淀）

### 今日待办提醒

- 真机安装提醒：手机 adb 通道（Tailscale/USB）恢复后安装 20:55 最新包（含时间戳新格式 + 转盘两改版）
- 服务端 health：`{"ok":true,"version":"0.13.0","sessions":78,"work":{"activity":"0.12.0 激活与真机验收","pending":4}}`

## 今日建议优先处理

1. **中断语义**——已决策、子代理实施中；弹框二选一 + 自动续跑直接影响 A 节多项真机验收，建议跟进实施进度并纳入最新包复验
2. **发送状态（IM 模式）**——已决策、子代理实施中；与「运行中仅排队不上屏」语义修正（`1eb461e`）联动，建议合并回归后一并真机验收
3. **装机进度显示**——已决策、子代理实施中；落地后可让每次真机装包全程可见（包大小/传输百分比/步骤/错误归类），直接服务当前待验收批次
