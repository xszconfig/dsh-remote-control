# 每日早报 · 2026年9月12日

## 一、待人工验收
- [ ] APK 瘦身（新增 release-in-house + release-store 两 buildType，R8 + arm64-only）—— `df750a9`：release-in-house 2.16MB（debug 12.6MB → −83%，目标 ≤6.3MB），模拟器冒烟 + 连接/hello/序列化/日志页/问题弹层回归通过，待真机装机验收。
- [ ] 时间戳新格式（前缀 + 时刻恒显：今天/昨天/前天/M月d日/跨年）—— `688257e`：单测 22 例 + 模拟器 UI 已验证，待真机装机后验收。
- [ ] 转盘改版：左移 Deep Diving 上方 + 56dp 五线小转盘图标 —— `0543a32`：模拟器验证，待真机目验位置/图标。
- [ ] 转盘方向调转（顺时针=更老 / 逆时针=更新）—— `b4af84e`：模拟器日志证据验证，待真机手感验收。
- [ ] 新消息不打断转盘（非收起态挂起强制滚底）—— `868b547`：门控代码验证，待真机实测（自动化实测受会话事件流干扰）。
- [ ] 行为日志埋点全覆盖（发送/中断/输入/列表/翻页/转盘/日志页）—— `42740d5`：日志证据验证，待抽查。
- [ ] Markdown 宽表格横滑 —— `86f6c68`：5 条单测，待人工目验（此前锁屏未复验）。
- [ ] 跳到底部按钮回归 —— `4184b59`：待目验。
- [ ] 子代理列表元信息 + 滚动 —— `fc1dfe19`：待目验。

## 二、做完未自测
- 主动通知与审批提醒（本地通知 v1）—— `9d0a4c6`：三态门控（前台浏览该会话不发/前台其它会话/后台锁屏发）+ 审批/提问高优幂等（approvalId/rpcId）+ 结果交付 D1/D2/D3（会话×轮次幂等）+ 夜间勿扰 22:00–08:00 仅静默结果交付 + 不合并；单测 20 例 + detektP0 全绿；⏳ 待设备自测（模拟器：审批/提问后台通知、浏览态抑制、结果交付、去重/勿扰）。

## 三、调研完待决策
- **Jugg 无头编译服务 MVP** —— 已决策：实施 MVP（2-5 人日）；子代理实施中（先设计后实现）。
- **Kotlin LSP** —— 已定案（2026-09-06）：维持官方 263.4421.0，待上游发布含 LSP-1561 修复的构建后按 UPGRADE-NOTES 升级（三实现对比论证见 docs/decisions/kotlin-lsp-selection.md）。
- **中断语义** —— 已决策：有排队消息时弹框二选一（终止并清空 / 仅终止保留，加取消）；「仅终止保留」后队列仍有待执行消息时自动启动新一轮 Agent 循环消费；子代理实施中。
- **发送状态（IM 模式）** —— 已决策：点发送立即上屏 + 同时间行 Loading；成功消失；失败红色❗点击重发；输入框立即清空；子代理实施中。
- **装机进度显示** —— 已决策：全程可见（包大小/传输百分比/步骤/错误归类）；子代理实施中。
- **R4 规则接入 lint** —— 已实施：checkDexRegisters 任务挂 assemble 自动执行（>128 告警 >256 失败），buildSrc 纯函数解析器 5 例单测。
- **goal 完成态隐藏** —— 已决策：对齐 DSH Web 隐藏；排队实施中。
- **推 GitHub** —— 已决策：暂不推（等全部验收后再定）；台账存在重复条目「等待用户明确指示」，以暂不推为准。
- **TS core.ts 3 处 lint 豁免拆分**（apply/handleCommand/projectEvent）—— 收紧路径已登记 `docs/lint-rules.md`。

## 四、昨日工作衔接
### 仓库提交
- **app**（23 条，2026-09-11 08:12 ~ 2026-09-12 08:12）：输入区重构 + 技能面板落地（`563a7e3`）、键盘发送按钮非空回车即发（`ac83df4`）、模型入口文案对齐服务端（`c29907c`）、子代理嵌套浏览 + 多级返回链（`c82122a`/`11c484c`）、FGS 前台服务不启动修复（`c7f14a6`）、BridgeClient 类体拆分合回 main + P0 LargeClass 阈值收紧 600（`42397fa`/`01f14da`）、技能面板半屏/高度约束与搜索恢复（`604a4b5`/`6d9b834`）、模型入口强度恒显不截断（`8cc6797`）、多条 PRD/验收报告/截图文档落库。
- **bridge**（7 条）：技能目录订阅推送 skills_update（v0.17.0 `9d94f96`）、技能目录读 preset 层全集修复只显示 1 个技能（v0.17.1 `4181d00`）、模型目录变更实时广播（`c967848`）、上下文占用 percent 与桌面端完全一致（`495e736`）、子会话模型入口继承父会话（`10873f2`）、冷会话上下文占用投影缓存冷读（`6f71464`）、无投影数据静态 contextWindow 兜底（`f4c9fb4`）；当前服务端版本 0.17.1。

### 新增 bug 文档
- `docs/bugs/2026-09-11-fgs-foreground-misjudged.md` —— FGS 前台服务不启动：前台态误判后台（后台线程 addObserver 抛异常 + combine 缺前台态触发源），修复 `c7f14a6`。

### 今日待办提醒
- 真机安装提醒：手机 adb 通道（Tailscale/USB）恢复后安装 20:55 最新包（含时间戳新格式 + 转盘两改版），并复核中断弹框竞态修复 `ceb5c05`。
- 新近上线的流程/工具（知晓即可）：`startup-smoke-test`、`code-lint` + pre-commit P0 闸门、`docs/coding-rules/verifyerror-deep-dive.md`、`docs/feature-priority-map.md`、工具调用超时修复。
- 服务端 `/remote/health`：ok，版本 0.17.1，sessions 95，work pending 4（活动：0.12.0 激活与真机验收）。

## 今日建议优先处理
1. **中断语义** —— 已决策（弹框二选一 + 自动续跑），子代理实施中；与 A 节转盘/排队项直接关联，推动落地并完成设备自测。
2. **发送状态（IM 模式）** —— 已决策，子代理实施中；发送链路是待真机验收批次的核心体验，推动「立即上屏 + Loading + 失败重发」闭环自测。
3. **装机进度显示** —— 已决策，子代理实施中；今日真机装机验收依赖全程可见的装机进度（包大小/传输百分比/步骤/错误归类）。
